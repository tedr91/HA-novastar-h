send_raw_command:
  name: Send Raw Command
  description: Send a raw API command to the Novastar processor (requires "Allow Raw Commands" option in configuration). Host is optional when only one Novastar device is configured.
  fields:
    host:
      name: Host
      description: Optional IP address of the Novastar processor. Required when multiple Novastar devices are configured.
      required: false
      example: "192.168.1.100"
      selector:
        text:
    endpoint:
      name: Command
      description: The API command endpoint path (e.g., "device/readDetail", "screen/writeBrightness").
      required: true
      example: "device/readDetail"
      selector:
        text:
    body:
      name: Body
      description: Optional JSON body to send with the request. If omitted, deviceId and screenId default to 0.
      required: false
      example: '{"deviceId": 0}'
      selector:
        object:

set_layer_source:
  name: Set Layer Source
  description: Set or clear the source routed to a specific layer. Host is optional when only one Novastar device is configured.
  fields:
    host:
      name: Host
      description: Optional IP address of the Novastar processor. Required when multiple Novastar devices are configured.
      required: false
      example: "192.168.1.100"
      selector:
        text:
    layer_id:
      name: Layer ID
      description: Target layer ID.
      required: true
      example: 2
      selector:
        number:
          min: 0
          mode: box
    input_id:
      name: Input ID
      description: Input source ID to route. Omit to clear source.
      required: false
      example: 1
      selector:
        number:
          min: 0
          mode: box
    interface_type:
      name: Interface Type
      description: Input interface type. Defaults to 0.
      required: false
      example: 5
      selector:
        number:
          min: 0
          mode: box
    slot_id:
      name: Slot ID
      description: Input slot/interface ID. Defaults to 0.
      required: false
      example: 1
      selector:
        number:
          min: 0
          mode: box
    crop_id:
      name: Crop ID
      description: Crop ID. Use 255 for original source.
      required: false
      example: 255
      selector:
        number:
          min: 0
          mode: box

set_active_preset:
  name: Set Active Preset
  description: Set the active preset on a Novastar processor. Host is optional when only one Novastar device is configured.
  fields:
    host:
      name: Host
      description: Optional IP address of the Novastar processor. Required when multiple Novastar devices are configured.
      required: false
      example: "192.168.1.100"
      selector:
        text:
    preset_id:
      name: Preset ID
      description: Preset ID to activate.
      required: true
      example: 1
      selector:
        number:
          min: 0
          mode: box

get_screen_details:
  name: Get Screen Details
  description: Get screen details from screen/readDetail. Host is optional when only one Novastar device is configured.
  fields:
    host:
      name: Host
      description: Optional IP address of the Novastar processor. Required when multiple Novastar devices are configured.
      required: false
      example: "192.168.1.100"
      selector:
        text:
    device_id:
      name: Device ID
      description: Device ID. Defaults to 0.
      required: false
      example: 0
      selector:
        number:
          min: 0
          mode: box
    screen_id:
      name: Screen ID
      description: Screen ID. Defaults to 0.
      required: false
      example: 0
      selector:
        number:
          min: 0
          mode: box

get_input_details:
  name: Get Input Details
  description: Get input details from input/readDetail. Host is optional when only one Novastar device is configured.
  fields:
    host:
      name: Host
      description: Optional IP address of the Novastar processor. Required when multiple Novastar devices are configured.
      required: false
      example: "192.168.1.100"
      selector:
        text:
    device_id:
      name: Device ID
      description: Device ID. Defaults to 0.
      required: false
      example: 0
      selector:
        number:
          min: 0
          mode: box
    input_id:
      name: Input ID
      description: Input ID to read.
      required: true
      example: 0
      selector:
        number:
          min: 0
          mode: box

get_output_details:
  name: Get Output Details
  description: Get output details from output/readDetail. Host is optional when only one Novastar device is configured.
  fields:
    host:
      name: Host
      description: Optional IP address of the Novastar processor. Required when multiple Novastar devices are configured.
      required: false
      example: "192.168.1.100"
      selector:
        text:
    device_id:
      name: Device ID
      description: Device ID. Defaults to 0.
      required: false
      example: 0
      selector:
        number:
          min: 0
          mode: box
    output_id:
      name: Output ID
      description: Output ID to read.
      required: true
      example: 0
      selector:
        number:
          min: 0
          mode: box

get_layer_details:
  name: Get Layer Details
  description: Get layer details from layer/readDetail. Host is optional when only one Novastar device is configured.
  fields:
    host:
      name: Host
      description: Optional IP address of the Novastar processor. Required when multiple Novastar devices are configured.
      required: false
      example: "192.168.1.100"
      selector:
        text:
    device_id:
      name: Device ID
      description: Device ID. Defaults to 0.
      required: false
      example: 0
      selector:
        number:
          min: 0
          mode: box
    screen_id:
      name: Screen ID
      description: Screen ID. Defaults to 0.
      required: false
      example: 0
      selector:
        number:
          min: 0
          mode: box
    layer_id:
      name: Layer ID
      description: Layer ID to read.
      required: true
      example: 0
      selector:
        number:
          min: 0
          mode: box

get_preset_details:
  name: Get Preset Details
  description: Get preset details from preset/readDetail. Host is optional when only one Novastar device is configured.
  fields:
    host:
      name: Host
      description: Optional IP address of the Novastar processor. Required when multiple Novastar devices are configured.
      required: false
      example: "192.168.1.100"
      selector:
        text:
    device_id:
      name: Device ID
      description: Device ID. Defaults to 0.
      required: false
      example: 0
      selector:
        number:
          min: 0
          mode: box
    screen_id:
      name: Screen ID
      description: Screen ID. Defaults to 0.
      required: false
      example: 0
      selector:
        number:
          min: 0
          mode: box
    preset_id:
      name: Preset ID
      description: Optional preset ID to read. If omitted, the currently active preset is used.
      required: false
      example: 0
      selector:
        number:
          min: 0
          mode: box
